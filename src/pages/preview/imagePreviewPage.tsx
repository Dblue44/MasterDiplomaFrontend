import {PreviewWidget} from "@widgets/previewWidget";

export const ImagePreviewPage = () => {

  return (
    <div className="flex w-full min-w-0 flex-col gap-6 p-4 lg:p-6">
      <PreviewWidget/>
    </div>
  );
};
